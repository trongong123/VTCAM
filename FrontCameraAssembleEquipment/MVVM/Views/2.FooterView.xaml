<UserControl x:Class="FrontCameraAssembleEquipment.MVVM.Views.FooterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FrontCameraAssembleEquipment.MVVM.Views"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:FrontCameraAssembleEquipment.MVVM.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:FooterViewModel}"
             d:DesignHeight="100" d:DesignWidth="800">
    <DockPanel Background="WhiteSmoke">
        <Grid Height="2.5"
              DockPanel.Dock="Bottom" Background="{StaticResource PrimaryBackgroundColor}"/>
        <Grid Height="2.5"
              DockPanel.Dock="Top"/>
        <ContentControl Content="{Binding NavigateVM}"
                        Visibility="{Binding IsInitializing, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Height="85"/>
    </DockPanel>
</UserControl>
