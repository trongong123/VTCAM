<Window x:Class="FrontCameraAssembleEquipment.MVVM.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontCameraAssembleEquipment.Resources.Controls"
        xmlns:ui="clr-namespace:EQX.UI.Controls;assembly=EQX.UI"
        mc:Ignorable="d"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent"
        Padding="10"
        MouseDown="Window_MouseDown"
        Loaded="Window_Loaded"
        Title="LoginView" Height="380" Width="300">
    <Border CornerRadius="4" AllowDrop="True" Background="White" BorderThickness="1" BorderBrush="Gray" Margin="4" >
        <Border.Effect>
            <DropShadowEffect Color="Silver"
                    Opacity="0.5"
                    BlurRadius="3"/>
        </Border.Effect>
        <StackPanel Width="250" VerticalAlignment="Center" Orientation="Vertical" HorizontalAlignment="Center">
            <Image Margin="0 50 0 0" Width="130" Source="{StaticResource image_NV_SubUI_Login_Image}" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant"/>

            <Label FontWeight="Bold"
                   Margin="0 10 0 0"
                   Content="{DynamicResource str_Password}" />

            <PasswordBox VerticalContentAlignment="Center"
                         HorizontalContentAlignment="Center"
                         Name="passwordBox"
                         Height="40"
                         Focusable="True"
                         PreviewMouseDown="passwordBox_PreviewMouseDown">
                <PasswordBox.Style>
                    <Style TargetType="{x:Type PasswordBox}">
                        <Style.Setters>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <Border BorderBrush="{StaticResource PrimaryBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="5"
                                                Background="{TemplateBinding Background}">
                                            <Grid>
                                                <ScrollViewer
                                                    x:Name="PART_ContentHost"
                                                    HorizontalScrollBarVisibility="Hidden"
                                                    VerticalScrollBarVisibility="Hidden" />
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style.Setters>
                    </Style>
                </PasswordBox.Style>
            </PasswordBox>

            <Border Margin="0 10 0 0" CornerRadius="6" Panel.ZIndex="1" Background="{StaticResource PrimaryBackgroundColor}">
                <Button Content="{DynamicResource str_Login}"
                        Style="{StaticResource ActionButtonStyle}"
                        BorderThickness="0"
                        Height="40" Foreground="White"
                        FontSize="15" FontFamily="Consolas"
                        Background="Transparent"
                        Click="LoginButton_Click"/>
            </Border>

            <Button Style="{StaticResource ActionButtonStyle}" Margin="20" Content="Close" VerticalAlignment="Center" HorizontalAlignment="Center"
                    BorderThickness="0" Command="{Binding CloseLoginViewCommand}" />
        </StackPanel>
    </Border>
</Window>
