<UserControl x:Class="FrontCameraAssembleEquipment.Resources.Controls.CamTrayCellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cvt="clr-namespace:EQX.UI.Converters;assembly=EQX.UI"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:FrontCameraAssembleEquipment.Resources.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Button Command="{Binding CellClickedCommand}" Margin="2"
            PreviewMouseDoubleClick="Button_MouseDoubleClick">
        <!--<i:Interaction.Triggers>
            <i:EventTrigger EventName="PreviewMouseDoubleClick">
                <i:InvokeCommandAction Command="{Binding CellDoubleClickedCommand}"
                                       PassEventArgsToCommand="True"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>-->
        <Button.Style>
            <Style  BasedOn="{StaticResource {x:Type Button}}" TargetType="{x:Type Button}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <!-- Set Border Background to prevent click through button -->
                            <Border BorderBrush="Black"
                                    BorderThickness="1.5"
                                    Background="{Binding Path=Status, Converter={StaticResource CellStatusToColorConverter}}"
                                    CornerRadius="3">
                                <Image Stretch="Uniform" RenderTransformOrigin="0.5,0.5"
                                       Source="{Binding Path=Status, Converter={StaticResource CellStatusToImageConverter}}">
                                    <Image.RenderTransform>
                                        <ScaleTransform ScaleX="-1"/>
                                    </Image.RenderTransform>
                                </Image>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

                <Style.Triggers>
                    <Trigger
                        Property="IsEnabled"
                        Value="False">
                        <Setter
                            Property="Foreground"
                            Value="Black"/>

                        <Setter
                            Property="Cursor"
                            Value="Arrow"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
    </Button>
</UserControl>
