<UserControl x:Class="FrontCameraAssembleEquipment.Resources.Controls.ReadBarcode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FrontCameraAssembleEquipment.Resources.Style"
             xmlns:inout="clr-namespace:EQX.InOut;assembly=EQX.InOut"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="400">

    <Grid>
        <Border BorderBrush="Gray" CornerRadius="5" BorderThickness="1" Margin="5">
            <StackPanel Margin="5">
                <TextBlock Text="[BARCODE READER]" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,5"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource ActionButtonStyle}" 
                            Content="ReadBarcode"
                            Click="RunClick"/>
                    <Separator Width="35" Height="1" Margin="3 0">
                        <Separator.LayoutTransform>
                            <RotateTransform Angle="90"/>
                        </Separator.LayoutTransform>
                    </Separator>
                    <TextBox IsReadOnly="True" x:Name="txtbox_Barcode"
                             Width="150" Height="30" Margin="5"
                             VerticalContentAlignment="Center"
                             FontSize="12" FontWeight="Bold"
                             TextAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
