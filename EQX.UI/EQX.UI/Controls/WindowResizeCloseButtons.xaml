<UserControl x:Class="EQX.UI.Controls.WindowResizeCloseButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EQX.UI.Controls"
             Name="root"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:WindowResizeCloseButtons}"
             Height="30"
             Width="120">

    <UserControl.Resources>
        <Style TargetType="{x:Type Button}"
               BasedOn="{StaticResource ContentOnlyButtonStyle}">
            <Style.Setters>
                <Setter Property="Padding" Value="6"/>
                <Setter Property="Margin" Value="1 0 0 1"/>
            </Style.Setters>
        </Style>
    </UserControl.Resources>
    
    <UniformGrid Rows="1">
        <Button Click="MinimizeButtonClick">
            <Image Source="{StaticResource ImageWindowMinimizeDefault}"/>
        </Button>
        <Button Click="MaximizeButtonClick">
            <Image Source="{StaticResource ImageWindowMaximizeDefault}"/>
        </Button>
        <Button Command="{Binding CloseButtonClickCommand}">
            <Button.Content>
                <Image Source="{StaticResource ImageWindowCloseDefault}"/>
            </Button.Content>
            <Button.Style>
                <Style TargetType="{x:Type Button}"
                       BasedOn="{StaticResource {x:Type Button}}">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource ImageButtonMouseOverHightLightColor}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </UniformGrid>
</UserControl>
