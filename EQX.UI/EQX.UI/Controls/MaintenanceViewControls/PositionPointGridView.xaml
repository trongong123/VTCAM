<UserControl x:Class="EQX.UI.Controls.PositionPointGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:EQX.UI.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:EQX.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400">
    <UserControl.Resources>
        <converters:DifferenceConverter x:Key="DifferenceConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>
    <Border BorderBrush="{StaticResource DarkBackgroundColor}" BorderThickness="1">
        <Grid x:Name="MainGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> <!-- Cột cho tiêu đề hàng -->
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/> <!-- Hàng cho tên trục -->
                <RowDefinition/> <!-- Hàng cho Fixed Pos -->
                <RowDefinition/> <!-- Hàng cho Offset Pos -->
                <RowDefinition/> <!-- Hàng cho Model Pos -->
                <RowDefinition/> <!-- Hàng cho Vision Pos -->
                <RowDefinition/> <!-- Hàng cho Target Pos -->
                <RowDefinition/> <!-- Hàng cho Current Pos -->
                <RowDefinition/> <!-- Hàng cho Difference -->
            </Grid.RowDefinitions>
        </Grid>
    </Border>
</UserControl>