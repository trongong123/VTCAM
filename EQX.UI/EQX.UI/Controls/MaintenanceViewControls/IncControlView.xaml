<UserControl x:Class="EQX.UI.Controls.IncControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EQX.UI.Controls"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             x:Name="root"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}"
               x:Key="JogButton">
            <Setter Property="Margin"
                    Value="0 2" />
            <Setter Property="Height"
                    Value="30" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="FontSize"
                    Value="14" />
        </Style>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <GroupBox Header="Inc Move"
                      Style="{x:Null}"
                      Grid.IsSharedSizeScope="True">
                <ItemsControl ItemsSource="{Binding ItemsSource, ElementName=root}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- DataContext ở đây là một đối tượng PositionPoint -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto"
                                                      SharedSizeGroup="MaintenanceJogLabelWidth" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Nút Jog - -->
                                <Button Grid.Column="0"
                                        Content="-"
                                        Style="{StaticResource JogButton}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="PreviewMouseUp">
                                            <i:InvokeCommandAction Command="{Binding DataContext.IncMinusCommand, ElementName=root}"
                                                                   CommandParameter="{Binding Motion}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>

                                <!-- Tên trục -->
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Motion.Name}"
                                           Margin="2 0"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           FontWeight="Bold" />

                                <!-- Nút Jog + -->
                                <Button Grid.Column="2"
                                        Content="+"
                                        Style="{StaticResource JogButton}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="PreviewMouseUp">
                                            <i:InvokeCommandAction Command="{Binding DataContext.IncPlusCommand, ElementName=root}"
                                                                   CommandParameter="{Binding Motion}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>
            <GroupBox Header="Inc Step"
                      FontWeight="Bold"
                      Style="{x:Null}"
                      Grid.Column="1">
                <TextBox Text="{Binding IncStep ,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged, ElementName=root }"
                         IsReadOnly="True"
                         Style="{StaticResource TextBoxWithDescriptionStyle}"
                         PreviewMouseUp="TextBox_MouseUp" />
            </GroupBox>
        </Grid>
    </ScrollViewer>
</UserControl>
